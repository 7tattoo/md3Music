# MD3Music 更新日志

各版本更新说明按 `## vX.Y.Z` 分组（与发布标签一致）。
发布时 CI 会自动取当前版本对应的节作为 GitHub Release 正文，并补上标题、构建时间、下载链接与页脚；
若某版本没有对应条目，则回退为自动生成的提交列表。

## v5.0.7

本次更新为「我的收藏」的专辑页补上了与歌单一致的批量删除能力。

### 功能

- 「我的收藏」专辑支持**长按批量删除**：长按专辑进入批量管理模式（与歌单完全一致），可多选后批量取消收藏，逐项统计成功/失败
- 批量管理模式下切换 tab 自动退出选择状态，避免歌单/专辑选中错位

## v5.0.6

本次更新修复了「我的收藏」删除歌单失败的问题，并完善了批量删除的结果反馈。

### 修复

- 修复「我的收藏」删除歌单一直失败（提示「删除失败，请稍后重试」）：本地服务器转发删除接口时补齐了酷狗网关必需的 `x-router` 路由头，删除请求不再被网关 502 拒绝
- 修正批量删除歌单的 `type` 参数：删除自己创建的歌单用 `type=1`，取消收藏别人的歌单用 `type=0`，避免删错对象

### 体验优化

- 「我的收藏」批量删除改为逐项判定结果并统计：部分成功时提示「成功删除 N 个，M 个失败」，不再静默

## v5.0.5

本次更新修复了歌词动画的功耗问题，并优化了签到流程。

### 性能与功耗

- 修复播放歌词时 CPU 占用高、耗电的问题：歌词收敛检测只检查视口附近行，动画结束后 Ticker 能正常停止，不再持续高负载

### 签到优化

- 支持酷狗 20028 二次安全验证码流程：签到/领取遇到安全验证时自动弹出验证码，验证通过后自动重试，补齐畅听 VIP + 概念版双签到

## v5.0.4

本次更新重点优化了性能与功耗：降低播放时的 CPU / 温度 / 耗电，并大幅缩短 App 冷启动等待时间。

### 性能与功耗优化

- 修复播放时手机 CPU 占用高、发热、耗电的问题：播放进度更新不再导致播放页面整体高频重建，歌词动画空闲时自动进入省电态
- 播放进度条仍平滑更新，切歌、歌词、投屏、通知等功能不受影响

### 启动速度优化

- App 冷启动转圈时间从 4 秒+ 缩短到约 1.5 秒：本地服务检测更快，发现页改为渐进加载（先显示已获取的分区，其余后台补充）

## v5.0.3

本次更新主要优化了歌单的加载体验。

### 歌单体验

- 歌单详情（热门歌单 / 我收藏 / 发现等入口）改为**点进即加载全部歌曲**，无需分页滚动加载，进入即可浏览和搜索完整歌单

## v5.0.2

本次更新重点统一了界面风格并大幅优化了滚动性能：所有一级页面改为统一的 iOS 分组卡片风格，修复了列表快速滑动时的空白/卡顿问题，主题色新增自定义取色。

### 一、界面风格统一

- 设置、我的、发现、本地音乐、排行榜、我收藏、私人FM、封面流、搜索、听歌识曲 全部统一为 iOS 风格：中性背景 + 圆角分组卡片 + 语义色，浅深色自动适配
- 一级页面顶部标题居中显示
- 主题色新增「自定义颜色」圆形调色盘，可自由选取任意颜色作为主题色
- 封面流去掉背景块，更清爽

### 二、性能优化

- 歌单详情改为分页加载：首屏一页、滚动到底部自动加载下一页，不再一次性拉取全部歌曲
- 修复列表快速滑动时内容空白、停下才显示的问题（根源：列表项随播放进度高频重建、以及逐项入场动画）
- 封面图改用缓存加载并限制解码尺寸，滚动更流畅

### 三、其它

- 本地音乐移入可选 Tab（可在设置页「主页管理」中开启）
- 云盘、搜索等页面细节优化

## v5.0.1

本次更新重点补全了云盘音乐能力，并在播放、投屏、歌词与评论区做了大量优化。底层架构也完成了一次重要升级：本地服务器改用 Rust 全新重写，App 更小、启动更快、登录更稳定。同时修复了 v5.0.0 中云盘上传失败的问题。

### 修复

- **修复云盘上传**：移除 v5.0.0 引入的上传判断误判逻辑，恢复所有歌曲上传到云盘，并保留上传失败时的友好提示。

### 一、架构升级（重要）

- **底层服务器全面重写**：本地 API 服务器改用 Rust 实现，覆盖 160+ 酷狗接口，行为与酷狗官方对齐。不再依赖旧的 Node.js 运行时，App 体积更小、启动更快。
- **登录更稳定**：登录、注册、验证码等环节改为本地直连酷狗，不再依赖第三方转发。修复了登录二维码扫码后无法通过校验（20010）的问题。
- **启动不再失败**：本地服务器改用随机端口，彻底解决端口被占用导致 App 启动失败的问题。

### 二、云盘音乐（重点补全）

- **上传本地歌曲**：云盘页新增上传入口，支持长按多选、批量上传，上传失败会明确提示原因。
- **云盘歌曲显示封面**：自动从音频文件中提取内嵌封面，云盘列表不再"无封面"。
- **云盘歌曲可投屏**：云盘歌曲也能投放到电视、音响等 DLNA 设备。
- **浏览与播放更流畅**：优化云盘列表加载与分页，修复云盘歌曲播放失败（405）与无法切换音质的问题。

### 三、播放与投屏

- **本地歌曲投屏**：支持将本地音乐投放到 DLNA 设备（电视、音响等），可拖动播放进度，封面与歌名正常同步。
- **睡眠定时关闭**：播放器新增"定时关闭"，支持固定档位与自定义时长，到点自动暂停；顶部栏可随时查看并修改剩余时间。
- **无版权歌曲也能播**：已收藏的无版权歌曲可通过兜底链路正常播放；播放列表遇到无法播放的歌曲会自动跳过，不再卡死。
- **播放体验优化**：音质显示更真实（统一为"hq"），默认音质调整为 128kbps；拖动进度条时自动暂停，避免误听；搜索结果去重，同名歌曲不再重复展示。

### 四、歌词与视觉

- **逐字歌词更流畅**：大幅优化 Apple Music 风格歌词的渲染性能，字内渐变、上浮动画不再卡顿，视觉更细腻。
- **背景流光更好看**：优化动态背景取色逻辑，冷暖配色更协调，避免画面整体偏蓝绿。

### 五、评论区

- 热门评论与歌手评论置顶展示，带徽章标识
- 支持楼中楼评论分页加载、展开与收起
- 超长评论支持展开/收起动画
- 歌曲菜单新增"看评论"入口，评论面板实现统一
- 新增评论显示设置，可调整评论字号

### 六、问题修复

- 修复竖屏退出沉浸模式后状态栏不恢复的问题
- 修复播放列表页收藏按钮显示错误
- 修正"边听边存"默认状态（现默认为关闭）
- 更新用户协议页面的免责声明与内容
- 修正播放历史页相关收藏状态显示

### 七、工程与文档

- 新增 Windows 一键构建脚本，自动检测 Rust 变更并交叉编译
- 更新 README 与相关文档，适配新的架构说明
